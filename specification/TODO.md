# ToDo list

---

## 🟦 PHASE 1: V2 リリース（現在進行中：安定化・凍結優先）

### APIリファクタ（安定化）

- [✅] **APIリネームと統合**
- [✅] `MeasurementRead` トレイトのメソッドを `read_measurement` に変更
- [✅] `CdcReceiver` 内部のバイト読み取りを `read_raw_frame` にリネーム
- [✖] 戻り値を `String` から `Measurement` 構造体へ変更
  - ※既存経路との整合性チェックを優先

---

### ロジックのカプセル化（既存方針維持）

- [✅] `CdcReceiver::read_measurement` 内で Str/Bin の判定を完結させる
- [✅] `data_receiver`（呼び出し側）の `match` 分岐を撤去し、シンプルにする
- [ ] `data_receiver` 周辺の責務整理（必要最小限）

---

### Sim（シミュレータ）の整理

- [ ] Simのスレッド構造の整理（削除ではなく維持 or 簡素化の判断）
- [ ] `MeasurementRead` trait との整合性維持
- [ ] 同期化は実機との整合性を壊さない範囲で検討

---

### 表示・出力のクリーンアップ

- [ ] GUIモード時のターミナル出力（println!）抑制
- [ ] デバッグ出力のON/OFF方針を決める（暫定はコメントでもOK）
- [ ] GUIに単位変換機能追加（UI責務の範囲内で実装）

---

## 🟨 PHASE 2: V3（構造進化フェーズ：段階導入）

※設計メモ＋小さく試すフェーズ（まだ大規模変更禁止）

---

### 🧩 イベントモデル（軽量導入）

- [ ] `DigimaticEvent` の定義（最小構成）
  - `DataReceived`
  - `ErrorOccurred`
  - `MeasurementUpdated`（必要なら）

⚠ 注意：
- いきなりPub/Subにはしない
- まずはenumで流すだけ

---

### 🧩 データフロー整理（準備フェーズ）

- [ ] pipelineの責務整理（直列処理は維持）
- [ ] データ受信後の行き先整理
  - GUI
  - CLI
  - Logger（CSV）

- [ ] イベント化は設計準備のみ（まだ導入しない）

---

### 🧩 表示・出力の分離準備

- [ ] terminal / GUI / log の責務を明確化
- [ ] 出力系を将来分離できる構造に整理（まだ分離しない）

---

### 🧩 GUI拡張（最小変更）

- [ ] 現状構造を維持したまま最適化
- [ ] event-driven前提のインターフェース整理のみ

---

## 🟩 長期構想メモ（実装禁止ゾーン）

- DB導入（V3以降）
  - CSVは生ログとして維持（変更しない）
  - DBは補助ストレージ（後付け）

- Storage abstraction（必要になってから導入）
  - CSV / DB の差し替え構造

- Pub/Sub完全移行（現時点では導入しない）
  - 過剰設計リスクあり

---

## 🧠 設計ルール（重要）

- V2は「壊さないこと」を最優先
- V3は「構造変更の準備」が目的
- 抽象化は必要になってからのみ導入する